# vim

## workstation

```
git clone --recursive https://github.com/anordhoff/vim.git ~/vim
ln -s ~/vim ~/.config/vim
cd ~/vim; make init
```

## server

```
git clone --recursive https://github.com/anordhoff/vim.git ~/.vim
```
